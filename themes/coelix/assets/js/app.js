$('.slick-vertical').slick({
    vertical: true,
    verticalSwiping: true,

    slidesToShow: 1,

    speed: 500,
    // autoplay: true,
    dots: true,

    prevArrow: $('.btn-next'),
    nextArrow: $('.btn-prev'),
});
